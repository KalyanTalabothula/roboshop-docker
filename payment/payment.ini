[uwsgi]
wsgi-file = payment.py
callable = app

master = true
processes = 1
lazy-apps = true
enable-threads = true

socket = 0.0.0.0:8080
protocol = http

# Add these to run as non-root user safely
# uid = 1000
# gid = 1000